# SBCPUFloating V3.1.14 — 真正 Powerd 满血充电注入

本版本基于 V3.1.13，新增独立 `SBCPUPowerd.dylib`，目标进程明确为 `powerd`。

## 变更
- `SBCPUPowerd.xm` 独立编译为 `SBCPUPowerd.dylib`。
- `SBCPUPowerd.plist` 仅过滤 `powerd`，不会把该 Hook 注入 SpringBoard 或 thermalmonitord。
- 读取与设置页相同的 `forceFastChargeEnable` 偏好。
- 同时 Hook `IORegistryEntrySetCFProperty` 与 `IOServiceSetCFProperty`，仅针对充电电流/功率限制属性进行拦截。
- 开启时将 `AppleSmartBattery` 的 `ChargeLimit` 设置为 100；关闭时尝试恢复开启前读到的值。
- 通过 Darwin notification + 2 秒低频兜底检查同步开关状态。

## 注意
“满血充电”会绕过部分原厂充电降流/热保护策略，可能提高温度和电池损耗。建议只在散热良好、明确知道风险时测试。

## 重要
该版本解决的是“是否真正注入 `powerd`”这一工程结构问题；实际设备上的具体充电属性名称仍取决于 iOS 17.0 / A16 的 `powerd` 实现，因此安装后应以日志和实际充电电流/功率验证。
