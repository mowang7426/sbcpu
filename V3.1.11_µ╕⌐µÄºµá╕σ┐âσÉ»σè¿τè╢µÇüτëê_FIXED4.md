# V3.1.11 温控核心启动状态 FIXED4

本版针对 GitHub Actions 编译错误进行修复。

## 修复内容
- 删除未被调用的 `sbcputhermalCoreActuallyLoaded()` 及其整套 thermalmonitord/libproc 跨进程模块扫描代码。
- 不再因为 `-Werror,-Wunused-function` 导致 `Tweak.xm` 编译失败。
- 温控保护总开关打开后：显示“温控：核心启动中”。
- 等待约 8 秒后：显示“温控：核心已运行”。
- 温控保护关闭后：显示“温控：保护已关闭”。
- 保留原有温控压力、高温保护、低功耗恢复等实际控制逻辑。
- 不修改 SBCPU 主 CPU/FPS/电量/温度/通知等功能。

注意：本版的“核心已运行”是按用户要求采用启动状态方案显示，不再通过 SpringBoard 跨进程扫描 thermalmonitord 的 dylib 映射来判断。
