# SBCPUFloating V3.1 — Control Center

基于 `sbcpu-main` V3.1 工程重新制作。

## 本版新增

- `SBCPUFloatingCC.bundle`
- 安装路径：`/Library/ControlCenter/Bundles`
- 模块显示名称：`SB CPU Floating`
- 点击模块直接切换 `isEnabled`
- 使用与 SpringBoard Tweak 相同的 CFPreferences App ID 与 Darwin notification：
  - `com.yourname.sbcpufloating`
  - `com.yourname.sbcpufloating.prefschanged`
- 开启/关闭后立即通知 SpringBoard，浮窗状态同步。
- 模块同时兼容基于 CCSupport 机制的控制中心加载器；用户设备使用 CCLess 时可由 CCLess 扫描该 Bundle。

## 注意

控制中心第三方模块的显示依赖设备上安装并启用支持第三方模块的控制中心加载器（例如 CCSupport/CCLess）。Theos 当前自带 `iphone/control_center_module-11up` 模板；本模块采用同样的 `/Library/ControlCenter/Bundles` Bundle 部署路线。
