# SBCPUFloating V3.1.10

本版本只修复温控核心“已注入但仍显示核心未运行”的检测误判。

## 检测逻辑

- 目标进程固定为 `thermalmonitord`
- 通过 `libproc.dylib` 找到 thermalmonitord PID
- 优先使用 `proc_pidinfo(..., PROC_PIDREGIONPATHINFO, ...)` 直接读取每个内存区域对应的文件路径
- 同时保留 `proc_regionfilename()` 作为兼容兜底
- 只要路径中检测到 `SBCPUThermal.dylib`，即认为核心已经加载
- 不再依赖 heartbeat / notify 来判断“核心是否加载”
- 现有温控控制逻辑未修改

截图验证标准：
`thermalmonitord -> Modules -> .../TweakInject/SBCPUThermal.dylib`
应对应浮窗/设置页的“温控：核心已运行”。
