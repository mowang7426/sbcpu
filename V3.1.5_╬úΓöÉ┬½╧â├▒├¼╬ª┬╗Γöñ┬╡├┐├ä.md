# SBCPUFloating V3.1.5

## 本版重点

### 1. 修复“温控核心未运行”误判
RootHide 环境下，SpringBoard 与 thermalmonitord 可能读取不同的 `/var/tmp` 路径。
本版统一使用当前 RootHide `.jbroot-*` 路径解析心跳文件，让两个进程读取同一份心跳。

同时：
- 设置页面继续每秒刷新；
- 悬浮窗继续每秒刷新；
- 只要最近 10 秒收到真实核心心跳，就显示“温控：核心运行中”；
- 即使启动完成通知在 RootHide 下不同步，也不会再把已经存活的核心误报成“未运行”。

### 2. 充电功能
本版不新增绕过原厂温度/电流保护的“强制快充”代码。
保留安全的“充电增强”思路：显示真实电池侧充电功率，并可把充电目标设为 100%。
实际充电电流/功率仍由系统、温度、电池、充电器和线材共同决定。

## 测试重点
安装后注销/重载一次，等待约 10 秒，然后观察：
- 设置 → 温度保护 → “温度保护当前状态”
- 悬浮窗 → “温控”

正常情况下应该从“检测中/未运行”变为“核心运行中”或“正常”。
